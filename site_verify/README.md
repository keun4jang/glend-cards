# 검색엔진 소유권 확인 파일

구글 서치콘솔 / 네이버 서치어드바이저에서 받은 확인용 HTML 파일을
이 폴더에 그대로 넣으면, 사이트를 빌드할 때마다 자동으로 docs/ 루트에 복사된다.

- 구글: `google1234abcd.html` 같은 파일
- 네이버: `naver1234abcd.html` 같은 파일

docs/는 빌드 산출물이라 거기에 직접 넣으면 다음 빌드에서 사라진다.
반드시 이 폴더에 넣을 것.
